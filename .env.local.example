NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Transactional email (staff invites, etc). Optional — features that send
# email degrade gracefully (e.g. staff invites fall back to a copy-link) if
# these are unset. Requires a Resend account with a verified sending domain.
RESEND_API_KEY=
RESEND_FROM=
